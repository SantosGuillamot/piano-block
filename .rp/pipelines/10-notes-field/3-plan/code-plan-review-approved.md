# Code Plan Review

## Verdict: approved

## Summary

This is a strong, fully standalone plan and it is now sound and implementable — every blocker from the prior three review rounds is genuinely resolved against the real worktree. The v2 must-fix (the Task 5 ↔ Task 8 standalone-stacking contradiction) is closed: Task 5's `measureModel.standaloneNotes` record now carries the **raw-`beat` group key** (the raw `beat` as-is, or a precomputed `group`/`stackIndex` for `(staff, placement, raw beat ?? "noBeat")`), the record-shape wording (Changes, plan lines 149/156–161) and the closed acceptance list (line 169) agree, the insufficient "array order alone" escape hatch is explicitly removed and replaced with the proof of why resolved-X/array-order is insufficient (the `beat:50`/`beat:99` clamp-collision counterexample, line 157/170), and Task 8 (line 262) now stacks "from the Task 5 record's stored raw-`beat` group key alone — do NOT re-read `measure.notes` and do NOT derive the group from the resolved X." Both tasks' acceptance now assert the same-`(staff, placement, raw beat)` grouping AND the over-content-clamp counter-check (Task 5 line 170, Task 8 lines 277–278), so two sequential code-writers cannot diverge. The two already-resolved v1 issues are not regressed: the constants size-rename stays green at every step (Task 2 adds `NOTE_SIZE` and keeps `CHORD_SYMBOL_SIZE` as a temporary alias; Task 3 switches the two importers to `NOTE_SIZE` *first*, then deletes the alias *last*, with the grep gate proving it is gone), and the Task 5 over-content clamp remains coordinate-frame-consistent in the measure-relative `scaledContent` frame. I re-verified every cited code/test/doc anchor against the worktree, confirmed the validator-unchanged claim, confirmed selector-collision safety, and confirmed the unit baseline is green (214 tests, 5 suites). Spec coverage and traceability are complete, the ordering is build-safe and green at each step, and acceptance criteria are observable/testable. No new genuine blocker exists.

## Verification performed (against the worktree)

- **V2 must-fix resolved.** Task 5 Changes (lines 149, 156–161) store the raw-`beat` group key with three equivalent forms; acceptance (lines 169–170) asserts the field exists, that same-`(staff, placement, raw beat)` notes share a group, that `beat:2` vs `beat:2.0001` differ, and the `beat:50`/`beat:99` same-clamped-X counter-check. Task 8 (line 262, acceptance 277–278) consumes the group key from the record alone — no `measure.notes` re-read, no resolved-X grouping. The Task 5 ↔ Task 8 contradiction is gone.
- **V1 issues not regressed.** (a) Constants: only `constants.js` (def), `svg.js` (import line 32, use 867), `layout.js` (import line 33, use 2279) reference `CHORD_SYMBOL_SIZE` — exactly the two importers the plan claims; Task 2 keeps the alias, Task 3 switches-then-deletes, Task 2 acceptance requires the full suite green. (b) Task 5 clamp uses `scaledContent` (relative right edge); `measureRightX = x + scaledContent` at `layout.js:1804` is confirmed absolute, validating the frame-mismatch rationale.
- **Code anchors accurate.** `schema.js`: `chordSymbol` at `event.properties:145`, `if`/`then`+`biome-ignore` at 151–153, `measure.properties` 72–84 (no `notes` collision). `constants.js`: `CHORD_SYMBOL_SIZE=2.8` at 158, `INTRA_STAFF_GAP=8` at 78, `SYSTEM_BOTTOM_MARGIN=5` at 87, `TEXT_LANE_GAP=0.6` at 97. `layout.js`: `collectEventTexts` 1550–1557 (read 1554, push `kind:"chordSymbol"` 1555), `band` literal 1688–1701 (`chordSymbolY` 1700), `bottomMargin` 1682, `lhTopY=rhBottomY+INTRA_STAFF_GAP` 1684, measure-walk scope 1753–1855 (`leadInset` 1760, `scaledContent=leadInset+scaledGrid` 1768, `columnX` from `cx=leadInset` 1773, `ml.columns` 1774, `ml.measureEnd` exists 805/829, `advanceScale`, `measureModels.push` 1842–1851 with `right/left/barlines/inline`), `systemHasChordSymbols` 2239, `topMarginLayout` 2260–2299. `svg.js`: `renderMeasure` 469 + `translate(measure.x 0)` 471 (no Y translate), `chordDyR/L` 479–486, `renderHand` 514 + `translate(0 staffBottomY)` 516, `renderHandText` 845–872 (`font-size:CHORD_SYMBOL_SIZE` 867, `data-text:"chord-symbol"` 869, `chordDy ?? -5` 865), barline absolute-X-then-subtract 496–498 (the convention the plan correctly says standalone notes do NOT follow).
- **Test/doc anchors accurate.** `layout.test.js` 844/1314/1487/1488/1530; `svg.test.js` 29/119/134; `render.spec.js` 48/73/215/219/230/343/478 (`HOSTILE_CHORD` at 219 does not contain the token, so the grep gate is met without renaming it); `editor.spec.js` 51/54/66; `docs/song-format.md` 167/178/183/298; `README.md` 162. The 12 token-carrying files match the design's enumeration exactly.
- **Validator unchanged holds.** `validate.js` has `enum` (120), inclusive `minimum` (140, `value < minimum`), `required` (183), and per-element paths via `validateArray` (153/166) — every new rule is declarative; `beat ≥ 0` is `minimum:0` and `beat`'s float values pass the `typeof === "number"` check.
- **Selector-collision safety.** `data-kind="note"` (noteheads, `svg.js:547`) ≠ `data-text="note"` (different attribute); `data-staff-lines` (326) ≠ `data-staff` (different attribute); all text via `setText` (textContent) → XSS-inert.
- **Green baseline.** `npm run test:unit` → 214 passed, 5 suites.
- **Coverage + traceability.** Every spec requirement (1–20) and every acceptance criterion maps to a task; the coverage map (lines 305–315) is honest about the Docs-phase split for Requirement 8 (migration-line content); per-task acceptance is observable/existence-or-relative/`toBeCloseTo`, never test-prescriptive; ordering is build-safe and green at each step.

## Notes (non-blocking, no change required)

- The plan refers to a "Text sizes / vertical gaps" region in `constants.js`; the file actually has two regions ("System wrapping / justify / vertical gaps" at line 72 and "Text sizes, in sp" at line 152). The placement guidance is unambiguous about which constant goes where (`NOTE_SIZE` next to the other text sizes; the four geometry constants among the vertical gaps), so this is a harmless prose imprecision, not an error.
- Task 3 correctly distinguishes the emit-primitive `kind` (currently `"chordSymbol"` at `layout.js:1555`, becoming `"note"`) from the DOM `data-text` value (currently `"chord-symbol"` at `svg.js:869`, becoming `"note"`); the retargeted assertions (e.g. `layout.test.js:1314` `t.kind === "chordSymbol"`) match the real values. No ambiguity for the writer.
- All four prior-round blockers (v1 ×2, v2 ×1) are settled; do not re-litigate. Constant tuning, font/family decisions, and the migration-line content are correctly deferred (constants to Task 2 starting values within design ranges; migration content to the Docs phase).
