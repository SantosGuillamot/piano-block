# Docs review — Review 3 (batch DT1–DT4): APPROVED

**Verdict:** APPROVED.
**Batch:** DT1–DT4 of `3-plan/doc-plan.md`.
**Diff base:** `c241cb6`. **Doc commits:** `19dd3f6` (DT1) → `b2c3508` (DT2) → `263fb15` (DT3) → `1034596` (DT4), touching only `README.md` and `docs/song-format.md` (no `src/` changes in the batch).

## Rationale

The docs batch accurately and proportionally documents the shipped review-3 behavior, with no remaining instance of the prior rejection's defect class (canvas/staff click-to-select) and no front-end drift. Verified against the spec, design doc, doc plan, and — critically — the shipped code.

- **Accuracy vs shipped code.** The left structure tree is documented as the navigate/select surface; the canvas as display + highlight only. Confirmed against `src/editor/StructureTree.js` (Section → Measure → Right/Left hand → Note rows, `Section %d`/`Measure %d` positional fallbacks, per-hand "Add note", hand-group rows organizational/non-selecting), `src/edit.js` (the "Structure" `ToolbarButton` bound to `showTree`; lifted add/remove/duplicate mutators; `StructureTree` rendered in the `__workspace` beside `SongCanvas`), and `src/editor/SongCanvas.js` (no `interactive` flag — display-only). The right-sidebar `StructureList` is genuinely gone (no file, no doc reference). The note-language selector and raw-JSON surface are documented unchanged. The new optional `name` field is present in `src/song/schema.js` on both `$defs.section` and `$defs.measure` (string, not required, permissive), and the rename `TextControl` is in both `SectionPanel.js`/`MeasurePanel.js` dropping the key when blank — matching the docs' "clearing the field restores the positional fallback."
- **Drift-resistance (critical) — clean.** `name` is documented exactly like `language`: stored, round-trips, validates permissively/never-blocking, and **front end does not consume it** ("ignores `name` … storing or changing it changes nothing about the rendered notation"). No doc claims any front-end behavior for `name`. The contributor `interactive` note ("The now-dormant `interactive` hit-rect") is accurate: `git diff c241cb6..HEAD -- src/notation/svg.js` is empty (byte-unchanged), neither `SongCanvas.js` nor `view.js` passes the flag (verified by grep), and the README states "svg.js was not changed and the flag was not deleted" — the minimal revert, stated precisely with no false "svg.js changed / flag deleted" claim.
- **No stale claims.** Every "staff"/"canvas" mention in both docs is either correctly negated ("clicking the staff does not select", e2e "clicking the canvas does not select") or unrelated (annotation rendering; the accurate dormant-flag note). The README "Forthcoming" opener now reads "Authoring now happens through a structure tree beside the canvas … the canvas is the live render with selection highlighting," retaining the gradual-dynamics/audio content. The song-format "Intro and mental model" paragraph is reframed to the tree-selects / canvas-highlights split with the raw-JSON alternative, no-audio, front-end-SVG, and README cross-links intact. The old "tab + Enter/Space" canvas-select affordance is fully removed. No leftover right-sidebar Structure-list references. Reordering appears only as the correct negation ("Reordering is not available — the tree has no move controls"); the lone other "reorder" is the unrelated `render.php` re-escaping note.
- **Links/anchors.** README → `docs/song-format.md#name` resolves to the new `## \`name\`` heading (GitHub slug `name`); `#language` and `#annotated-example-song` resolve. Reverse links from song-format resolve: `../README.md#using-the-piano-block`, `#4-what-the-front-end-shows`, and `#2-build-the-song-in-the-visual-editor` all match real headings.
- **Completeness vs doc-plan.** DT1 (status blurb + "What the block does today"), DT2 (full "Using the Piano block" rewrite — tree-driven add/remove/duplicate, rename via the Section/Measure Name field, name-or-positional + note/chord/rest labels with no octave promised, Structure-list and canvas-click removed), DT3 (file-layout rows for `src/edit.js`/`src/editor/`, the dormant-`interactive` subsection, additive-growth `name`, tests paragraph), and DT4 (song-format Intro reframe, `name?` in both shape blocks, the `name` subsection, additive-growth note) are each delivered. Changes are proportional to `README.md` + `docs/song-format.md` only — no new docs, no inline code-symbol API reference, no pipeline references. The annotated example song is unchanged (no diff lines within it).

No actionable defects found. Approved.
