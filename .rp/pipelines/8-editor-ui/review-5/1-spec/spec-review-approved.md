# Spec Review

## Verdict: approved

## Reviewer

Owner (assisted workflow)

## Notes

Review-5 spec approved. The owner supplied the three requirements directly (with annotated screenshots) and directed autonomous execution of the whole pipeline (Design → Docs). Scope: make the tree's per-row action buttons (remove/duplicate/add) clearly visible with Gutenberg/theme styling; remove the tree's "Add section" button and add an add-section control to the block-settings sidebar; and make the selected-note highlight genuinely small (root-causing why the current `outline` on the note SVG `<g>` renders large under the notation scale). Editor-side only: no song format/schema, render.php, or front-end SVG change.
